void main() {
    int x = readInt("Enter an integer: ");
    int y = readInt("Enter another integer: ");

    for (int i = 0; i < y; i++) {
        if (i == x) {
            print("|");
        } else {
            print("-");
        }
    }
}

void main() {
    int num = readInteger("Enter a number");
    for (int i = 0; i <= 12; i++) {
        if (i >= 10) {
            println(i + " x " + num + " = " + (i * num));
        } else {
            println(i + "  x " + num + " = " + (i * num));
        }
    }
}
